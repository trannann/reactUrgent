const tagyInitialState = {
    tagy: 'tag'
}

const TagyReducer = (state = tagyInitialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default TagyReducer;